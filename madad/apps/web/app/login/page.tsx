'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
import { API_URL } from '../../lib/api';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('admin@madad.sa');const [password,setPassword]=useState('Madad@123');const [error,setError]=useState('');const [loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch(`${API_URL}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await r.json();if(!r.ok)throw new Error(data.error||'تعذر تسجيل الدخول');localStorage.setItem('madad_access_token',data.accessToken);localStorage.setItem('madad_user',JSON.stringify(data.user));router.push('/dashboard')}catch(e){setError(e instanceof Error?e.message:'حدث خطأ')}finally{setLoading(false)}}
return <div className="login-wrap"><form className="login-card form" onSubmit={submit}><div className="eyebrow">KADANA-READY FIELD OPERATIONS</div><h1 className="title">مَدَد</h1><p className="subtitle">منصة تنسيق البلاغات والفرق والموارد الميدانية في المشاعر.</p>{error&&<div className="error">{error}</div>}<label>البريد<input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>كلمة المرور<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn" disabled={loading}>{loading?'جاري الدخول...':'دخول مركز العمليات'}</button><div className="muted" style={{fontSize:12}}>حساب العرض: admin@madad.sa / Madad@123</div></form></div>}
