'use client';import {useEffect,useState} from'react';import AppShell from'../../components/AppShell';import{api}from'../../lib/api';export default function Page(){const[r,s]=useState<any[]>([]);useEffect(()=>{api<any[]>('/teams').then(s)},[]);return <AppShell><div className="topbar"><div><div className="eyebrow">WORKFORCE</div><h1 className="title">الفرق الميدانية</h1></div></div><div className="grid grid-3">{r.map(t=><div className="card" key={t.id}><div className="row" style={{justifyContent:'space-between'}}><strong>{t.name}</strong><span className={`badge ${String(t.status).toLowerCase()}`}>{t.status}</span></div><p className="muted">{t.specialization} · {t.site?.name}</p><div>{t.skills?.map((x:string)=><span className="badge" key={x} style={{margin:'3px'}}>{x}</span>)}</div><p>المهام الحالية: {t.activeJobs}/{t.maxConcurrentJobs}</p></div>)}</div></AppShell>}
