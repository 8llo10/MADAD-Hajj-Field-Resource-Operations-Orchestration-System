'use client';import{useEffect,useState}from'react';import AppShell from'../../components/AppShell';import{api}from'../../lib/api';export default function Page(){const[r,s]=useState<any[]>([]);useEffect(()=>{api<any[]>('/inventory').then(s)},[]);return <AppShell><div className="topbar"><div><div className="eyebrow">INVENTORY CONTROL</div><h1 className="title">المخزون التشغيلي</h1></div></div><div className="grid grid-3">{r.map(x=><div className={`card ${x.quantity<=x.reorderLevel?'recommend':''}`} key={x.id}><div className="muted">{x.sku}</div><h3>{x.name}</h3><div className="score">{x.quantity} <span style={{fontSize:14}}>{x.unit}</span></div><p className="muted">حد إعادة الطلب: {x.reorderLevel}</p>{x.quantity<=x.reorderLevel&&<span className="badge high">يتطلب إعادة طلب</span>}</div>)}</div></AppShell>}
