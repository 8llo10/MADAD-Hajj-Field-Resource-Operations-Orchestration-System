"use client";import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {token} from '@/lib/api';export default function AuthGate({children}:{children:React.ReactNode}){const r=useRouter();const [ok,setOk]=useState(false);useEffect(()=>{if(!token())r.replace('/login');else setOk(true)},[r]);return ok?children:<div style={{padding:30}}>Loading MADAD…</div>}
