"use client";import Link from 'next/link';import {usePathname,useRouter} from 'next/navigation';import {clearToken} from '@/lib/api';
const nav=[['/','Command Center'],['/incidents','Incidents'],['/dispatch','Dispatch'],['/teams','Teams'],['/resources','Resources'],['/inventory','Inventory'],['/reports','Reports'],['/audit','Audit']];
export default function Shell({children}:{children:React.ReactNode}){const p=usePathname(),r=useRouter();return <div className="app"><aside className="sidebar"><div className="brand">مَدَد <small>MADAD · FIELD OPERATIONS</small></div><nav className="nav">{nav.map(([href,label])=><Link key={href} href={href} className={p===href?'active':''}>{label}</Link>)}</nav><button className="btn ghost" style={{marginTop:30,width:'100%'}} onClick={()=>{clearToken();r.push('/login')}}>Sign out</button></aside><main className="main">{children}</main></div>}
