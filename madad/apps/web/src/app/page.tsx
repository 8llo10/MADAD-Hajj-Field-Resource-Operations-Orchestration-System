"use client";import {useEffect,useState} from 'react';import Shell from '@/components/Shell';import AuthGate from '@/components/AuthGate';import {api,token} from '@/lib/api';import {io} from 'socket.io-client';
export default function Home(){const[d,setD]=useState<any>();useEffect(()=>{const load=()=>api('/operations/dashboard').then(setD).catch(()=>{});load();const socket=io(process.env.NEXT_PUBLIC_SOCKET_URL||'http://localhost:4000',{auth:{token:token()}});['incident.created','incident.updated','dispatch.accepted','dispatch.completed'].forEach(e=>socket.on(e,load));return()=>{socket.disconnect()}},[]);const k=d?.kpis||{};return <AuthGate><Shell><div className="top"><div><h1>Operations Command Center</h1><div className="muted">Live operational picture across holy sites</div></div><div className="badge AVAILABLE">SYSTEM ONLINE</div></div><div className="grid kpis">{[['Open incidents',k.openIncidents],['Critical',k.criticalIncidents],['Available teams',k.availableTeams],['Busy teams',k.busyTeams],['Resolved 24h',k.resolved24h],['Avg resolution',`${k.avgResolutionMinutes||0} min`]].map(([a,b])=><div className="card kpi" key={a as string}><span className="muted">{a}</span><b>{b??'—'}</b></div>)}</div><div className="grid split"><div className="card"><div className="section-title"><h2>Recent incidents</h2></div><div className="table-wrap"><table className="table"><thead><tr><th>Ref</th><th>Incident</th><th>Site</th><th>Severity</th><th>Status</th><th>AI risk</th></tr></thead><tbody>{d?.recentIncidents?.map((x:any)=><tr key={x.id}><td>{x.reference}</td><td>{x.title}</td><td>{x.site.nameEn}</td><td><span className={`badge ${x.severity}`}>{x.severity}</span></td><td><span className={`badge ${x.status}`}>{x.status}</span></td><td>{Math.round((x.aiRiskScore||0)*100)}%</td></tr>)}</tbody></table></div></div><div className="card ai"><div className="section-title"><h2>Operational AI</h2></div><p>Explainable risk and SLA models are active. Scores use severity, site criticality, crowd density, age, affected people and field capacity.</p><div className="muted">AI outputs are advisory; dispatch actions remain auditable and operator-controlled.</div></div></div></Shell></AuthGate>}
