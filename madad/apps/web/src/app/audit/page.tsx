"use client";import {useEffect,useState} from 'react';import Shell from '@/components/Shell';import AuthGate from '@/components/AuthGate';import {api} from '@/lib/api';export default function Audit(){const[x,setX]=useState<any[]>([]);useEffect(()=>{api<any[]>('/audit').then(setX).catch(()=>setX([]))},[]);return <AuthGate><Shell><div className="top"><div><h1>Audit Trail</h1><div className="muted">Who changed what, when and from where</div></div></div><div className="card table-wrap"><table className="table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Entity</th><th>ID</th><th>IP</th></tr></thead><tbody>{x.map(a=><tr key={a.id}><td>{new Date(a.createdAt).toLocaleString()}</td><td>{a.actor?.name||'System'}</td><td>{a.action}</td><td>{a.entityType}</td><td>{a.entityId?.slice(-8)||'—'}</td><td>{a.ip||'—'}</td></tr>)}</tbody></table></div></Shell></AuthGate>}
