"use client";import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {api,setToken} from '@/lib/api';export default function Login(){const r=useRouter();const[email,setEmail]=useState('admin@madad.sa'),[password,setPassword]=useState('Madad@123'),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{const x=await api<any>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});setToken(x.accessToken);r.push('/')}catch(e:any){setError(e.message)}}return <div className="login"><form className="login-card" onSubmit={submit}><h1>مَدَد</h1><p className="muted">Operations Command Center</p><div className="grid"><input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="btn">Sign in</button>{error&&<div className="error">{error}</div>}<small className="muted">Demo: admin@madad.sa / Madad@123</small></div></form></div>}
