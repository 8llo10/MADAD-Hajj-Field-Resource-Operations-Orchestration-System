import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import rateLimit from 'express-rate-limit'; import swaggerUi from 'swagger-ui-express'; import auth from './modules/auth/auth.routes.js'; import incidents from './modules/incidents/incident.routes.js'; import dispatch from './modules/dispatch/dispatch.routes.js'; import operations from './modules/operations/operations.routes.js'; import catalog from './modules/catalog/catalog.routes.js'; import { errorHandler } from './middleware/error.js'; import { swaggerSpec } from './swagger.js'; import { env } from './config.js'; import reports from './modules/reports.routes.js';
export function createApp(){const app=express();app.set('trust proxy',1);app.use(helmet());app.use(cors({origin:env.WEB_ORIGIN,credentials:true}));app.use(express.json({limit:'1mb'}));app.use(rateLimit({windowMs:60_000,limit:300}));app.get('/api/v1/health',(_q,res)=>res.json({status:'ok',service:'madad-api',time:new Date().toISOString()}));app.use('/docs',swaggerUi.serve,swaggerUi.setup(swaggerSpec));app.use('/api/v1/auth',auth);app.use('/api/v1/incidents',incidents);app.use('/api/v1/dispatch',dispatch);app.use('/api/v1/operations',operations);app.use('/api/v1',catalog);app.use('/api/v1',reports);app.use((_q,res)=>res.status(404).json({error:'Not found'}));app.use(errorHandler);return app}
