import { Router } from 'express'; import { z } from 'zod'; import { IncidentSeverity, IncidentStatus, Role } from '@prisma/client'; import { prisma } from '../../db.js'; import { asyncHandler } from '../../lib/async.js'; import { authenticate,authorize } from '../../middleware/auth.js'; import { audit } from '../../lib/audit.js'; import { AppError } from '../../lib/errors.js'; import { analyzeIncident } from '../ai/ai.service.js'; import { emitOps } from '../../realtime.js';
const router=Router();router.use(authenticate);
const createSchema=z.object({title:z.string().min(4),description:z.string().min(8),category:z.string().min(2),severity:z.nativeEnum(IncidentSeverity),siteId:z.string(),zoneId:z.string().optional().nullable(),latitude:z.number(),longitude:z.number(),peopleAffected:z.number().int().min(0).default(0),slaMinutes:z.number().int().min(5).default(60),requiredSkills:z.array(z.string()).default([]),requiredCapabilities:z.array(z.string()).default([]),source:z.string().default('COMMAND_CENTER')});
router.get('/',asyncHandler(async(req,res)=>{const status=req.query.status as IncidentStatus|undefined,severity=req.query.severity as IncidentSeverity|undefined;const q=String(req.query.q||'');const rows=await prisma.incident.findMany({where:{status,severity,OR:q?[{reference:{contains:q,mode:'insensitive'}},{title:{contains:q,mode:'insensitive'}}]:undefined},include:{site:true,zone:true,dispatches:{include:{team:true},orderBy:{proposedAt:'desc'},take:1}},orderBy:{reportedAt:'desc'},take:200});res.json(rows)}));
router.get('/:id',asyncHandler(async(req,res)=>{const row=await prisma.incident.findUnique({where:{id:req.params.id},include:{site:true,zone:true,dispatches:{include:{team:true,resources:{include:{resource:true}}}},statusEvents:true}});if(!row)throw new AppError(404,'Incident not found');res.json(row)}));
router.post('/',authorize(Role.ADMIN,Role.COMMANDER,Role.DISPATCHER,Role.SUPERVISOR),asyncHandler(async(req,res)=>{const p=createSchema.parse(req.body);const count=await prisma.incident.count();const reference=`INC-${new Date().getFullYear()}-${String(count+1).padStart(5,'0')}`;const site=await prisma.site.findUnique({where:{id:p.siteId}});const zone=p.zoneId?await prisma.zone.findUnique({where:{id:p.zoneId}}):null;if(!site)throw new AppError(400,'Invalid site');const availableTeamCount=await prisma.team.count({where:{siteId:p.siteId,status:'AVAILABLE'}});const ai=analyzeIncident({severity:p.severity,ageMinutes:0,crowdDensity:zone?.crowdDensity||3,siteCriticality:site.criticality,peopleAffected:p.peopleAffected,availableTeamCount,requiredSkills:p.requiredSkills.length});const row=await prisma.incident.create({data:{...p,reference,reportedById:req.user!.id,aiRiskScore:ai.riskScore,aiSlaBreachProbability:ai.slaBreachProbability,aiEstimatedResolutionMinutes:ai.estimatedResolutionMinutes,aiSummary:ai.summary,statusEvents:{create:{toStatus:'NEW',actorId:req.user!.id,note:'Incident created'}}}});await audit(req,'CREATE','Incident',row.id,undefined,row);emitOps('incident.created',row);res.status(201).json({...row,ai})}));
router.patch('/:id/status',authorize(Role.ADMIN,Role.COMMANDER,Role.DISPATCHER,Role.SUPERVISOR,Role.TECHNICIAN),asyncHandler(async(req,res)=>{const p=z.object({status:z.nativeEnum(IncidentStatus),note:z.string().max(500).optional()}).parse(req.body);const before=await prisma.incident.findUnique({where:{id:req.params.id}});if(!before)throw new AppError(404,'Incident not found');const timeFields:any={};if(p.status==='TRIAGED')timeFields.triagedAt=new Date();if(p.status==='RESOLVED')timeFields.resolvedAt=new Date();if(p.status==='CLOSED')timeFields.closedAt=new Date();const row=await prisma.$transaction(async tx=>{const updated=await tx.incident.update({where:{id:before.id},data:{status:p.status,...timeFields}});await tx.incidentStatusEvent.create({data:{incidentId:before.id,fromStatus:before.status,toStatus:p.status,note:p.note,actorId:req.user!.id}});return updated});await audit(req,'STATUS_CHANGE','Incident',row.id,before,row);emitOps('incident.updated',row);res.json(row)}));
export default router;
