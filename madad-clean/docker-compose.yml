services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: madad
    ports:
      - "5432:5432"
    volumes:
      - madad_pg:/var/lib/postgresql/data
volumes:
  madad_pg:
