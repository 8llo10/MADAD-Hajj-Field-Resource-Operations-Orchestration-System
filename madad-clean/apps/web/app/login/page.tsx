"use client";import { useState } from 'react';import { useRouter } from 'next/navigation';const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api/v1';export default function Login(){const router=useRouter();const[email,setEmail]=useState('dispatcher@madad.sa');const[password,setPassword]=useState('Madad@123');const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');const r=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok){setError('Login failed');return}const d=await r.json();localStorage.setItem('madad_token',d.accessToken);router.push('/dashboard')}return <div className="login"><form className="card" onSubmit={submit}><div className="brand">مَدَد</div><h1>Operations Login</h1><p className="muted">Command center access for authorized field operations staff.</p><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="critical">{error}</p>}<button type="submit">Enter Command Center</button></form></div>}
