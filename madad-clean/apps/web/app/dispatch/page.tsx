import Shell from '../../components/Shell';export default function Page(){return <Shell><div className="top"><div><h1>Dispatch</h1><span className="muted">Auto-propose or manually assign teams, then control dispatch lifecycle.</span></div></div><div className="card"><p>Use the Incidents screen to view transparent team rankings. The backend exposes auto proposal, manual proposal, acceptance, dispatched, arrived, cancelled and completed transitions.</p></div></Shell>}
