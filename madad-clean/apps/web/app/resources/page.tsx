"use client";import {useEffect,useState} from 'react';import Shell from '../../components/Shell';import {api} from '../../lib/api';type R={id:string;assetTag:string;name:string;type:string;status:string;site:{name:string}};export default function Page(){const[x,setX]=useState<R[]>([]);useEffect(()=>{api<R[]>('/resources').then(setX)},[]);return <Shell><div className="top"><h1>Resources</h1></div><div className="card">{x.map(r=><div className="incident" key={r.id}><div><b>{r.assetTag} · {r.name}</b><div className="muted">{r.type} · {r.site.name}</div></div><b>{r.status}</b></div>)}</div></Shell>}
