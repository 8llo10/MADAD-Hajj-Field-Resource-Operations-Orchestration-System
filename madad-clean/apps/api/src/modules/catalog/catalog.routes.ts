import { Router } from 'express';import { prisma } from '../../db.js';import { authenticate } from '../../middleware/auth.js';import { asyncHandler } from '../../lib/async.js';const r=Router();r.use(authenticate);r.get('/sites',asyncHandler(async(_q,res)=>res.json(await prisma.site.findMany({include:{zones:true},orderBy:{name:'asc'}}))));r.get('/notifications',asyncHandler(async(req,res)=>res.json(await prisma.notification.findMany({where:{userId:req.user!.id},orderBy:{createdAt:'desc'},take:50}))));r.get('/audit',asyncHandler(async(_q,res)=>res.json(await prisma.auditLog.findMany({include:{actor:{select:{name:true,email:true}}},orderBy:{createdAt:'desc'},take:200}))));export default r;
