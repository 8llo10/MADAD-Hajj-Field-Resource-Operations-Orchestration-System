import { Router } from 'express';import { ResourceStatus,Role } from '@prisma/client';import { z } from 'zod';import { prisma } from '../../db.js';import { authenticate,authorize } from '../../middleware/auth.js';import { asyncHandler } from '../../lib/async.js';import { pathId } from '../../lib/params.js';const r=Router();r.use(authenticate);r.get('/',asyncHandler(async(_q,res)=>res.json(await prisma.resource.findMany({include:{site:true},orderBy:{name:'asc'}}))));r.patch('/:id/status',authorize(Role.ADMIN,Role.COMMANDER,Role.SUPERVISOR),asyncHandler(async(req,res)=>{const id=pathId(req.params.id);const status=z.object({status:z.nativeEnum(ResourceStatus)}).parse(req.body).status;res.json(await prisma.resource.update({where:{id},data:{status}}))}));export default r;
