import { Router } from 'express';import { Role,TeamStatus } from '@prisma/client';import { z } from 'zod';import { prisma } from '../../db.js';import { authenticate,authorize } from '../../middleware/auth.js';import { asyncHandler } from '../../lib/async.js';import { pathId } from '../../lib/params.js';const r=Router();r.use(authenticate);r.get('/',asyncHandler(async(_q,res)=>res.json(await prisma.team.findMany({include:{site:true,zone:true,members:{include:{user:{select:{id:true,name:true,role:true}}}}},orderBy:{name:'asc'}}))));r.patch('/:id/status',authorize(Role.ADMIN,Role.COMMANDER,Role.DISPATCHER,Role.SUPERVISOR),asyncHandler(async(req,res)=>{const id=pathId(req.params.id);const status=z.object({status:z.nativeEnum(TeamStatus)}).parse(req.body).status;res.json(await prisma.team.update({where:{id},data:{status}}))}));export default r;
